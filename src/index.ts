/**
 * maw-bg plugin entry.
 *
 * Scaffold only — bg/list/tail/attach/kill/gc subcommands ship in follow-up
 * PRs after /team-agents 3-agent design review.
 */

export const manifest = {
  name: "bg",
  version: "0.0.1",
  description: "Run long commands in detached tmux; sample output non-destructively",
};

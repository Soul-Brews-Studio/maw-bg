import { describe, it } from "bun:test";

describe("maw-bg subcommands (skeleton)", () => {
  it.skip("bg auto-name: spawns detached tmux session with derived name", () => {});
  it.skip("bgList: enumerates active maw-bg sessions with status", () => {});
  it.skip("bgTail: samples last N lines non-destructively", () => {});
  it.skip("bgAttach: re-attaches to a named session", () => {});
  it.skip("bgKill: terminates session and cleans up resources", () => {});
  it.skip("bgGc: reaps stale/dead sessions older than threshold", () => {});
});
